# ========================================
# Docker Environment Variables
# ========================================
# Copia a .env y ajusta según necesites

NODE_ENV=production
LOG_LEVEL=info

# Database (MySQL)
DB_HOST=db
DB_PORT=3306
DB_USER=smartlining
DB_PASSWORD=password
DB_NAME=smartlining
DB_ROOT_PASSWORD=root

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_in_production_please
JWT_EXPIRES_IN=24h

# CORS
CORS_ORIGIN=http://localhost:3000

# Server
PORT=3000
